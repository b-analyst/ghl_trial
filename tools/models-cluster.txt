# Lineage-cluster sweep. 5 epochs x 9 variants = 45 episodes per model.
#
# The clusters are what can actually be OBSERVED -- lab, origin, and whether
# the weights are open. Training-data provenance is not public, so it cannot be
# stratified on; the point of the sweep is to see whether behaviour clusters
# along the observable lines at all. Cluster C already splits hard in
# logs/final (claude 0%, gemini 7.5%, grok 10%), so "frontier closed" is
# not predictive and a tight cluster A would be the real finding.
#
# Two entries are commented out because they resolve but serve no tool-calling
# endpoint. check_models.py now refuses a roster containing them, which is the
# check that was missing when they took a sweep down mid-run.
#
# Per-model rates at 45 episodes are noisy -- 20 claim cells each, ~2 expected
# hits at a 10% rate. Read the clusters, not the rows.

# --- A: open-weight, Chinese labs -------------------------------------------
deepseek/deepseek-v4-flash-0731
z-ai/glm-5.3-flash
minimax/minimax-m2

# --- B: open-weight, US/EU labs ---------------------------------------------
openai/gpt-oss-120b
meta-llama/llama-3.3-70b-instruct
# microsoft/phi-4 -- serves no tool-calling endpoint, cannot run this fixture
mistralai/mistral-small-3.2-24b-instruct
# nousresearch/hermes-4-70b -- same, no tool-calling endpoint

# --- C: closed, adds two cheap members to the three already in logs/final ---
openai/gpt-5-nano
cohere/command-r-08-2024
