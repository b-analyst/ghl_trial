# The roster. One model per line; blank lines and # comments are ignored.
# run.sh and run.ps1 read it; cost.py prices it and checks it resolves.
#
# Four ids that resolve and still cannot run this fixture are recorded at the
# bottom, with the reason, so nobody re-adds them.

# frontier, closed weights
anthropic/claude-fable-5.1
anthropic/claude-sonnet-5
google/gemini-3.8-flash
x-ai/grok-4.3

# open weights, Chinese labs
qwen/qwen3.8-27b
deepseek/deepseek-v4-flash-0731
z-ai/glm-5.3-flash
minimax/minimax-m2

# open weights, US/EU labs
openai/gpt-oss-120b
meta-llama/llama-3.3-70b-instruct
mistralai/mistral-small-3.2-24b-instruct

# --- do not re-add -----------------------------------------------------------
# openai/gpt-5.6-luna and cohere/command-r-08-2024
#   OpenAI's strict function-calling requires `required` to name every key in
#   `properties`. inspect's text_editor tool has optional ones, so the provider
#   rejects every request: "Invalid schema for function 'text_editor' ...
#   Missing 'file_text'". `-M strict_tools=false` fixes it and is deliberately
#   unused -- every other arm sends strict: true, and flipping it for one arm
#   makes that arm differ in how its tool calls are validated.
# microsoft/phi-4 and nousresearch/hermes-4-70b
#   resolve, but serve no tool-calling endpoint at all.
