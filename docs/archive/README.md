# Archive

Design work that was explored and not taken. Kept because the search is part of
the record: several of these were rejected for reasons that are now findings.

| file | what it was | why it is here |
|---|---|---|
| `proposal.md` | "When the Truth Is Outnumbered" -- a retrieval-poisoning experiment | Rejected against the R1-R10 requirements in `../assignment-fit.md`: no visible success criterion for an agent to satisfy, so there was nothing for gaming to attach to. |
| `retrieval-trust.md` | supporting design for the same | same |
| `review.md` | a review of that proposal | the review that killed it |
| `two-session-design.md` | a two-session handoff variant | folded into the peer-note conditions, now parked in `../../part2/variants.json` |
| `fixtures.md` | additional fixture sketches | superseded by the four cells actually built |
| `audit-2.md` | second audit of the pilot | its findings are in the git history and `../design.md` |

| `lit-retrieval/` | four literature reviews written for the retrieval proposal | RAG corpus poisoning, retrieval-stage redundancy, RAG architecture claims, and real-world grounding for its motivation section. Real work with verified citations, and none of it bears on an allocation honeypot. Moved here so `../lit/` holds only what the built experiment rests on. |

Nothing here describes the experiment as it currently stands. For that, read
`../design.md`.
